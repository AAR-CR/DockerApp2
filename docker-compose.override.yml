version: '3.4'

services:
  dockerapp2:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
    ports:
      - "8080"
#   nginx:
#     build:
#       context: .
#       dockerfile: Dockerfile.nginx
#     ports:
#       - "80:80"
#     depends_on:
#       - app
#     networks:
#       - webnet

# networks:
#   webnet: